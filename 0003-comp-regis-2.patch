From d5b99e1ce51ddcda26d24ad0665da5c5e7d6c6e8 Mon Sep 17 00:00:00 2001
From: chandrakanth pokuru <chandrakanth_pokuru2@comcast.com>
Date: Thu, 3 Sep 2026 14:54:27 -0400
Subject: [PATCH 03/17] comp regis 2

---
 __pycache__/layout.cpython-314.pyc       | Bin 55712 -> 55802 bytes
 architecture-standards.html              |   1 +
 component-registry.html                  |  64 ++++++++++++-
 components/all-components.json           |   2 +-
 components/ethwan-router-components.json |   2 +-
 components/ext-easymesh-components.json  |   2 +-
 components/ext-opensync-components.json  |   2 +-
 components/gw-components.json            |   2 +-
 components/gw-easymesh-components.json   |   2 +-
 components/gw-opensync-components.json   |   2 +-
 components/index.html                    |   3 +-
 components/modem-onu-components.json     |   2 +-
 gen_component_registry_page.py           | 117 ++++++++++++++++++-----
 hardware-compatibility.html              |   1 +
 index.html                               |   1 +
 industry-standards.html                  |   1 +
 layout.py                                |   1 +
 north-bound-apis.html                    |   1 +
 north-bound-specification.html           |   1 +
 south-bound-apis.html                    |   1 +
 technical-governance.html                |   1 +
 21 files changed, 173 insertions(+), 36 deletions(-)

diff --git a/__pycache__/layout.cpython-314.pyc b/__pycache__/layout.cpython-314.pyc
index 4a8a282d19aa4e08a3ce99c2a5be04449c9b4642..f9ed3fdb4f5af2cfff672178cb87a49f15dad9cb 100644
GIT binary patch
delta 218
zcmZ3mnfcddW?pSRUM>b8Fh4XiD{&*Q88eeg=4MxB5pKbp%)C_HjMU8Zj1nsaLp?L=
z$<?fqT8RY(sfk62dC93(3VHc?sn!a*<*7;8nI*dDGMj_>W~MT3oBX!;5aYJZ8%j7C
z8BcHCReFpCNHtd3F)}{ge4u(8Gmt8;H(+G^ws}QEF)JhAW|_894j^w*pFbO8=;B9H
zco{=CKVE3V$H=o;VS@%6kn-H7A;x%ObJ3Z{Y(OgPayctU9V;J81J?(i$yaZj1^_*{
BP|W}U

delta 153
zcmeyhnR&rxW?pSRUM>b8c)NRM7W+nCGiIh!8Jk_1MYuO_=9`?#xOK97$sxwAo4HCk
z85vJ)7AZT%0;FzN*)cLc*(_DFjTuN?tT$j}{JNQ`v6z*ScXL@=DF=}Erq7>^F=VmL
zR9?oA&9;kd_!zl2S8dQ>15%5(X^1f%-+boGV>TeQ;c_`EM=dKKO9R&jugUs1PXhpr
C?>T7z

diff --git a/architecture-standards.html b/architecture-standards.html
index c78eaca..0f2dee4 100644
--- a/architecture-standards.html
+++ b/architecture-standards.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/component-registry.html b/component-registry.html
index 8a9e801..8db92ab 100644
--- a/component-registry.html
+++ b/component-registry.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
@@ -421,10 +422,13 @@
   .cr-badge { display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px; border-radius: 999px;
     font-size: 0.78rem; font-weight: 600; border: 1px solid var(--border); }
   .cr-empty { text-align: center; color: var(--muted); padding: 32px 16px !important; }
+  .cr-catalog-note { font-size: 0.86rem; color: var(--muted); margin: -6px 0 20px; max-width: 720px; }
 </style>
 
 <script>
 const DATA_URL = "components/all-components.json";
+const GOV_URL = "component-governance.json";
+const GOV_NUMBERS = ["7.3.2", "7.3.2.1", "7.3.2.2", "7.3.3", "7.3.3.1"];
 
 function esc(s) {
   const d = document.createElement('div');
@@ -432,6 +436,47 @@ function esc(s) {
   return d.innerHTML;
 }
 
+// ---- governance rules (section 7.3.2 / 7.3.3), same renderer shape as
+// technical-governance.html so both pages look like one document ----
+function renderGovSections(sections) {
+  let html = '';
+  for (const s of sections) {
+    const level = s.level || 2;
+    const tag = level <= 2 ? 'h3' : (level === 3 ? 'h4' : 'h5');
+    html += `<div class="gov-section level-${level}">`;
+    html += `<${tag}><span class="gov-num">${esc(s.number)}</span><span>${esc(s.title)}</span></${tag}>`;
+    let listOpen = false;
+    for (const b of (s.blocks || [])) {
+      if (b.type === 'table') {
+        if (listOpen) { html += '</ul>'; listOpen = false; }
+        html += '<table class="def-table"><thead><tr>' + b.headers.map(h => `<th>${esc(h)}</th>`).join('') + '</tr></thead><tbody>' +
+          b.rows.map(r => `<tr>${r.map(c => `<td>${esc(c)}</td>`).join('')}</tr>`).join('') + '</tbody></table>';
+      } else if (b.type === 'li') {
+        if (!listOpen) { html += '<ul>'; listOpen = true; }
+        html += `<li>${esc(b.text)}</li>`;
+      } else {
+        if (listOpen) { html += '</ul>'; listOpen = false; }
+        html += `<p>${esc(b.text)}</p>`;
+      }
+    }
+    if (listOpen) html += '</ul>';
+    html += '</div>';
+  }
+  return html;
+}
+
+fetch(GOV_URL, { cache: 'no-store' })
+  .then(res => { if (!res.ok) throw new Error('HTTP ' + res.status); return res.json(); })
+  .then(data => {
+    const sections = (data.docs || []).filter(s => GOV_NUMBERS.includes(s.number));
+    document.getElementById('cr-gov').innerHTML = renderGovSections(sections);
+  })
+  .catch(() => {
+    document.getElementById('cr-gov').innerHTML =
+      '<p style="color:var(--muted);">Registry policy text (§7.3.2–§7.3.3) could not be loaded from ' + esc(GOV_URL) + '.</p>';
+  });
+
+// ---- catalog table (components/all-components.json) ----
 let ALL = [];
 let TIERS = [];
 let activeTier = null;
@@ -467,7 +512,7 @@ function renderTable() {
   });
 
   document.getElementById('cr-count').textContent =
-    rows.length === ALL.length ? `${ALL.length} components` : `${rows.length} of ${ALL.length} components`;
+    rows.length === ALL.length ? `${ALL.length} registered components` : `${rows.length} of ${ALL.length} registered components`;
 
   const body = document.getElementById('cr-tbody');
   if (!rows.length) {
@@ -559,14 +604,27 @@ fetch(DATA_URL, { cache: 'no-store' })
     <div class="hero-inner">
       <span class="eyebrow">Component Registry</span>
       <h1 style="font-size:2rem;">Component Registry</h1>
-      <p class="lede">Every RDK-B component in one place, independent of device profile -- name, category, classification tier, and source repository.</p>
-      <div class="badge-row"><span class="badge">Profile-independent catalog</span></div>
+      <p class="lede">The authoritative record of every RDK-B component -- registration, lifecycle state, and ownership, per §7.3.2 of the Core RDK Broadband Specification.</p>
+      <div class="badge-row"><span class="badge">§7.3.2 Component Registry</span><span class="badge">§7.3.3 Lifecycle States</span></div>
     </div>
     
   </div>
 </div>
 
 <section class="tight-top">
+  <div id="cr-gov"><div class="empty-state"><p>Loading registry policy…</p></div></div>
+</section>
+
+<section class="tight-top">
+  <div class="section-head">
+    <span class="eyebrow-lt">Registry data</span>
+    <h2>Registered Components</h2>
+  </div>
+  <p class="cr-catalog-note">
+    Currently populated fields: component name, category, source repository, and per-profile
+    classification tier. Owner, lifecycle state, HAL contract reference, TR-181 model, and
+    dependency fields from §7.3.2.1 are not yet tracked in the pipeline that feeds this table.
+  </p>
   <div id="cr-loading" class="empty-state"><p>Loading component catalog…</p></div>
   <div id="cr-content" style="display:none;">
     <div class="cr-toolbar">
diff --git a/components/all-components.json b/components/all-components.json
index f372a54..2f9050f 100644
--- a/components/all-components.json
+++ b/components/all-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:27Z",
+  "generatedAt": "2026-09-03T18:54:15Z",
   "title": "RDK-B Core Broadband Components",
   "subtitle": "Every component relevant to RDK-B Core Broadband across all device profiles -- Common Core, Required, or Optional for at least one profile.",
   "tiers": [
diff --git a/components/ethwan-router-components.json b/components/ethwan-router-components.json
index f750fa6..389588e 100644
--- a/components/ethwan-router-components.json
+++ b/components/ethwan-router-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:26Z",
+  "generatedAt": "2026-09-03T18:54:14Z",
   "title": "RDK-B EthWAN WiFi Router Components",
   "subtitle": "Components for the EthWAN WiFi Router device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/components/ext-easymesh-components.json b/components/ext-easymesh-components.json
index 63d47c9..d56dbba 100644
--- a/components/ext-easymesh-components.json
+++ b/components/ext-easymesh-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:27Z",
+  "generatedAt": "2026-09-03T18:54:15Z",
   "title": "RDK-B EXT EasyMesh Components",
   "subtitle": "Components for the EXT EasyMesh device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/components/ext-opensync-components.json b/components/ext-opensync-components.json
index ddd4ca7..60eeef7 100644
--- a/components/ext-opensync-components.json
+++ b/components/ext-opensync-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:27Z",
+  "generatedAt": "2026-09-03T18:54:15Z",
   "title": "RDK-B EXT OpenSync Components",
   "subtitle": "Components for the EXT OpenSync device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/components/gw-components.json b/components/gw-components.json
index 7763734..bf14cb8 100644
--- a/components/gw-components.json
+++ b/components/gw-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:26Z",
+  "generatedAt": "2026-09-03T18:54:14Z",
   "title": "RDK-B GW Components",
   "subtitle": "Components for the GW device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/components/gw-easymesh-components.json b/components/gw-easymesh-components.json
index 05e352c..1e21fcb 100644
--- a/components/gw-easymesh-components.json
+++ b/components/gw-easymesh-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:27Z",
+  "generatedAt": "2026-09-03T18:54:15Z",
   "title": "RDK-B GW EasyMesh Components",
   "subtitle": "Components for the GW EasyMesh device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/components/gw-opensync-components.json b/components/gw-opensync-components.json
index 4eaf962..cdb5b10 100644
--- a/components/gw-opensync-components.json
+++ b/components/gw-opensync-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:27Z",
+  "generatedAt": "2026-09-03T18:54:14Z",
   "title": "RDK-B GW OpenSync Components",
   "subtitle": "Components for the GW OpenSync device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/components/index.html b/components/index.html
index 4039d0f..9bf0c27 100644
--- a/components/index.html
+++ b/components/index.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
@@ -458,7 +459,7 @@
 
 <section class="tight-top">
   <p style="color:var(--muted); font-size:0.85rem; margin:0 0 14px;">
-    Schema version: 1.0 &nbsp;|&nbsp; Generated: 2026-09-03T18:39:26Z
+    Schema version: 1.0 &nbsp;|&nbsp; Generated: 2026-09-03T18:54:14Z
   </p>
   <div style="margin-bottom:18px;"><span class="pill" style="background:#fef3c7;color:#92400e">Common Core</span><span class="pill" style="background:#dbeafe;color:#1e40af">Required</span><span class="pill" style="background:#f3f4f6;color:#374151">Optional</span></div>
   <table class="def-table">
diff --git a/components/modem-onu-components.json b/components/modem-onu-components.json
index 192c847..6a59362 100644
--- a/components/modem-onu-components.json
+++ b/components/modem-onu-components.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "1.0",
-  "generatedAt": "2026-09-03T18:39:26Z",
+  "generatedAt": "2026-09-03T18:54:14Z",
   "title": "RDK-B Modem /ONU Components",
   "subtitle": "Components for the Modem /ONU device profile: Common Core, Required, and Optional.",
   "tiers": [
diff --git a/gen_component_registry_page.py b/gen_component_registry_page.py
index 7a2bad3..7abc545 100644
--- a/gen_component_registry_page.py
+++ b/gen_component_registry_page.py
@@ -1,24 +1,28 @@
 """One-purpose generator for component-registry.html.
 
-Renders a single, profile-agnostic component catalog -- every component
-across every RDK-B device profile, one row each, no per-profile split.
-Source of truth is components/all-components.json (already deduplicated
-across profiles by extract_components.py's all-profiles/merge step); all
-parsing and rendering happens client-side in the generated page, since
-all-components.json's shape ({schemaVersion, tiers, components}) doesn't
-fit the generic "flat array of records" loader in layout.render_stub_page
--- the tiers legend array would get picked up as "the data" instead of the
-78-entry components array.
-
-This intentionally carries a narrower field set than the full section 7.3.2
-Component Registry (name/category/tier/repo only, from the existing
-extraction pipeline) -- it's the profile-agnostic catalog view, not the
-governance registry with owner/lifecycle/HAL contract/dependencies. Those
-richer fields still need their own data source before they can render here
-or anywhere else.
+Two parts, in order:
+
+1. The registry rules themselves -- section 7.3.2 (Component Registry),
+   7.3.2.1 (Mandatory Registry Fields), 7.3.2.2 (Registry Maintenance
+   Rules), 7.3.3 (Component Lifecycle States), and 7.3.3.1 (State
+   Transition Rules) of the Core RDK Broadband Specification. This content
+   already exists in component-governance.json (it also backs the
+   "Component Governance Process" block on technical-governance.html) --
+   this page fetches that same file client-side and renders only the
+   7.3.2/7.3.3 subsection numbers, using the same .gov-section/.def-table
+   CSS classes technical-governance.html uses, so the two pages read as
+   one consistent design language.
+
+2. The catalog itself -- every component across every device profile, one
+   row each, from components/all-components.json. This is intentionally
+   a narrower field set than the full 7.3.2.1 mandatory field list (only
+   name/category/tier/repo, from the existing extraction pipeline) --
+   owner, lifecycle state, HAL contract, TR-181 model, and dependencies
+   still need their own data source before they can render here.
 
 Usage:
-    python3 gen_component_registry_page.py --data components/all-components.json --out-dir .
+    python3 gen_component_registry_page.py --data components/all-components.json \\
+        --governance component-governance.json --out-dir .
 """
 from __future__ import annotations
 
@@ -32,13 +36,17 @@ PAGE = {
     "active_id": "component-registry",
     "eyebrow": "Component Registry",
     "title": "Component Registry",
-    "lede": "Every RDK-B component in one place, independent of device profile -- name, "
-            "category, classification tier, and source repository.",
+    "lede": "The authoritative record of every RDK-B component -- registration, lifecycle "
+            "state, and ownership, per §7.3.2 of the Core RDK Broadband Specification.",
 }
 
+GOV_SECTION_NUMBERS = ["7.3.2", "7.3.2.1", "7.3.2.2", "7.3.3", "7.3.3.1"]
+
 SCRIPT_TEMPLATE = """
 <script>
 const DATA_URL = {data_url_json};
+const GOV_URL = {gov_url_json};
+const GOV_NUMBERS = {gov_numbers_json};
 
 function esc(s) {{
   const d = document.createElement('div');
@@ -46,6 +54,47 @@ function esc(s) {{
   return d.innerHTML;
 }}
 
+// ---- governance rules (section 7.3.2 / 7.3.3), same renderer shape as
+// technical-governance.html so both pages look like one document ----
+function renderGovSections(sections) {{
+  let html = '';
+  for (const s of sections) {{
+    const level = s.level || 2;
+    const tag = level <= 2 ? 'h3' : (level === 3 ? 'h4' : 'h5');
+    html += `<div class="gov-section level-${{level}}">`;
+    html += `<${{tag}}><span class="gov-num">${{esc(s.number)}}</span><span>${{esc(s.title)}}</span></${{tag}}>`;
+    let listOpen = false;
+    for (const b of (s.blocks || [])) {{
+      if (b.type === 'table') {{
+        if (listOpen) {{ html += '</ul>'; listOpen = false; }}
+        html += '<table class="def-table"><thead><tr>' + b.headers.map(h => `<th>${{esc(h)}}</th>`).join('') + '</tr></thead><tbody>' +
+          b.rows.map(r => `<tr>${{r.map(c => `<td>${{esc(c)}}</td>`).join('')}}</tr>`).join('') + '</tbody></table>';
+      }} else if (b.type === 'li') {{
+        if (!listOpen) {{ html += '<ul>'; listOpen = true; }}
+        html += `<li>${{esc(b.text)}}</li>`;
+      }} else {{
+        if (listOpen) {{ html += '</ul>'; listOpen = false; }}
+        html += `<p>${{esc(b.text)}}</p>`;
+      }}
+    }}
+    if (listOpen) html += '</ul>';
+    html += '</div>';
+  }}
+  return html;
+}}
+
+fetch(GOV_URL, {{ cache: 'no-store' }})
+  .then(res => {{ if (!res.ok) throw new Error('HTTP ' + res.status); return res.json(); }})
+  .then(data => {{
+    const sections = (data.docs || []).filter(s => GOV_NUMBERS.includes(s.number));
+    document.getElementById('cr-gov').innerHTML = renderGovSections(sections);
+  }})
+  .catch(() => {{
+    document.getElementById('cr-gov').innerHTML =
+      '<p style="color:var(--muted);">Registry policy text (§7.3.2–§7.3.3) could not be loaded from ' + esc(GOV_URL) + '.</p>';
+  }});
+
+// ---- catalog table (components/all-components.json) ----
 let ALL = [];
 let TIERS = [];
 let activeTier = null;
@@ -81,7 +130,7 @@ function renderTable() {{
   }});
 
   document.getElementById('cr-count').textContent =
-    rows.length === ALL.length ? `${{ALL.length}} components` : `${{rows.length}} of ${{ALL.length}} components`;
+    rows.length === ALL.length ? `${{ALL.length}} registered components` : `${{rows.length}} of ${{ALL.length}} registered components`;
 
   const body = document.getElementById('cr-tbody');
   if (!rows.length) {{
@@ -148,15 +197,29 @@ STYLE = """
   .cr-badge { display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px; border-radius: 999px;
     font-size: 0.78rem; font-weight: 600; border: 1px solid var(--border); }
   .cr-empty { text-align: center; color: var(--muted); padding: 32px 16px !important; }
+  .cr-catalog-note { font-size: 0.86rem; color: var(--muted); margin: -6px 0 20px; max-width: 720px; }
 </style>
 """
 
 
-def build_page(data_rel_url: str) -> str:
-    hero_badges = '<span class="badge">Profile-independent catalog</span>'
+def build_page(data_rel_url: str, gov_rel_url: str) -> str:
+    hero_badges = '<span class="badge">§7.3.2 Component Registry</span><span class="badge">§7.3.3 Lifecycle States</span>'
     body = render_hero(PAGE["eyebrow"], PAGE["title"], PAGE["lede"], hero_badges,
                         compact=True, visual_key=PAGE["active_id"]) + f'''
 <section class="tight-top">
+  <div id="cr-gov"><div class="empty-state"><p>Loading registry policy…</p></div></div>
+</section>
+
+<section class="tight-top">
+  <div class="section-head">
+    <span class="eyebrow-lt">Registry data</span>
+    <h2>Registered Components</h2>
+  </div>
+  <p class="cr-catalog-note">
+    Currently populated fields: component name, category, source repository, and per-profile
+    classification tier. Owner, lifecycle state, HAL contract reference, TR-181 model, and
+    dependency fields from §7.3.2.1 are not yet tracked in the pipeline that feeds this table.
+  </p>
   <div id="cr-loading" class="empty-state"><p>Loading component catalog…</p></div>
   <div id="cr-content" style="display:none;">
     <div class="cr-toolbar">
@@ -172,7 +235,11 @@ def build_page(data_rel_url: str) -> str:
 </section>
 '''
     head_extra = f"<title>{PAGE['title']} — RDK-B Core Broadband</title>\n" + STYLE + \
-        SCRIPT_TEMPLATE.format(data_url_json=json.dumps(data_rel_url))
+        SCRIPT_TEMPLATE.format(
+            data_url_json=json.dumps(data_rel_url),
+            gov_url_json=json.dumps(gov_rel_url),
+            gov_numbers_json=json.dumps(GOV_SECTION_NUMBERS),
+        )
     return render_page(PAGE["active_id"], head_extra, body)
 
 
@@ -180,13 +247,15 @@ def main() -> None:
     ap = argparse.ArgumentParser()
     ap.add_argument("--data", default="components/all-components.json",
                      help="Path (relative to the built site root) to the component catalog JSON.")
+    ap.add_argument("--governance", default="component-governance.json",
+                     help="Path (relative to the built site root) to the governance sections JSON.")
     ap.add_argument("--out-dir", default=".")
     args = ap.parse_args()
     out_dir = Path(args.out_dir)
     out_dir.mkdir(parents=True, exist_ok=True)
 
     path = out_dir / "component-registry.html"
-    path.write_text(build_page(args.data), encoding="utf-8")
+    path.write_text(build_page(args.data, args.governance), encoding="utf-8")
     print(f"Wrote {path}")
 
 
diff --git a/hardware-compatibility.html b/hardware-compatibility.html
index 5de7ede..3a80066 100644
--- a/hardware-compatibility.html
+++ b/hardware-compatibility.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/index.html b/index.html
index df987b2..35a43ab 100644
--- a/index.html
+++ b/index.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/industry-standards.html b/industry-standards.html
index 749c44e..e97eb04 100644
--- a/industry-standards.html
+++ b/industry-standards.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/layout.py b/layout.py
index f0a1644..0c01fb4 100644
--- a/layout.py
+++ b/layout.py
@@ -83,6 +83,7 @@ SHARED_CSS = """
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/north-bound-apis.html b/north-bound-apis.html
index cce1573..34da1b5 100644
--- a/north-bound-apis.html
+++ b/north-bound-apis.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/north-bound-specification.html b/north-bound-specification.html
index ce6dcb5..a127baf 100644
--- a/north-bound-specification.html
+++ b/north-bound-specification.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/south-bound-apis.html b/south-bound-apis.html
index 030f37f..0000f81 100644
--- a/south-bound-apis.html
+++ b/south-bound-apis.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
diff --git a/technical-governance.html b/technical-governance.html
index 671b6f5..94b458e 100644
--- a/technical-governance.html
+++ b/technical-governance.html
@@ -56,6 +56,7 @@
     display: flex; align-items: center; gap: 5px; cursor: pointer;
     background: linear-gradient(90deg, var(--rdk-blue), #7c3aed); border: none;
     font-family: inherit; color: #fff; text-decoration: none; font-size: 0.82rem; font-weight: 500;
+    line-height: 1.6; box-sizing: border-box; appearance: none; -webkit-appearance: none;
     padding: 7px 13px; border-radius: 999px; white-space: nowrap; transition: all 0.12s; outline: none;
   }
   .topnav > nav > a:hover, .nav-group-toggle:hover { filter: brightness(1.12); }
-- 
2.51.2

